import { Block } from "@/utils/types/notion";
import React from "react";

interface ColumnListBlockProps {
  block: Block;
}
export default function ColumnListBlock({ block }: ColumnListBlockProps) {
  return <div>ColumnListBlock</div>;
}
