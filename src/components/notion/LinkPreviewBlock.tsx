import { Block } from "@/utils/types/notion";
import React from "react";

interface LinkPreviewBlockProps {
  block: Block;
}
export default function LinkPreviewBlock({ block }: LinkPreviewBlockProps) {
  return <div>LinkPreviewBlock</div>;
}
