import { Block } from "@/utils/types/notion";
import React from "react";

interface TableRowBlockProps {
  block: Block;
}
export default function TableRowBlock({ block }: TableRowBlockProps) {
  return <div>TableRowBlock</div>;
}
